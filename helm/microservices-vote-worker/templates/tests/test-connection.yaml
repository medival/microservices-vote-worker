apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "microservices-vote-worker.fullname" . }}-test-connection"
  labels:
{{ include "microservices-vote-worker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "microservices-vote-worker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
